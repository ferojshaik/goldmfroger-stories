# Copy to project root .env when running "npm run server"
# Use a strong ADMIN_PASSWORD and set CORS_ORIGIN in production.

# Your frontend URL (HTTPS in production). No trailing slash.
# CORS_ORIGIN=https://your-frontend-domain.com

# PORT=3002

# Set in production so session cookie is Secure (HTTPS only).
# NODE_ENV=production
