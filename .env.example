# --- Frontend (Vite) ---
# Optional: API base URL if backend is not proxied (e.g. production).
# VITE_API_URL=https://your-api.com

# --- Backend (server/) - use in project root .env when running "npm run server" ---
# Required for owner login (Creator Studio). Use a long, random password (e.g. 20+ chars).
# Do NOT use a simple or default password in production.
ADMIN_PASSWORD=your-strong-random-password-here

# Production: set to your frontend URL so cookies work. No trailing slash.
# Example: https://yoursite.com  or  https://www.yoursite.com
CORS_ORIGIN=https://your-frontend-domain.com

# PORT=3002

# Production: set so the session cookie is sent with Secure (HTTPS only).
NODE_ENV=production

# --- Vercel (set in Vercel Dashboard → Project → Settings → Environment Variables) ---
# ADMIN_PASSWORD = same strong password as above (required for owner login)
# SESSION_SECRET = optional; if not set, ADMIN_PASSWORD is used to sign session cookies
# CORS_ORIGIN = your Vercel site URL, e.g. https://your-project.vercel.app (no trailing slash)
# NODE_ENV = production
